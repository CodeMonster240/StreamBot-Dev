{
  "name": "streambot-dev",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
    "postinstall": "npx puppeteer browsers install chrome"
    "start": "cd /home/container && if [ -f package.json ]; then npm install --no-fund --no-audit; fi && npx puppeteer browsers install chrome && node ${STARTUP_FILE}"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "discord.js": "^14.27.0",
    "dotenv": "^17.4.2",
    "puppeteer": "^25.3.0"
  }
}
